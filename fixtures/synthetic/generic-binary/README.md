# Synthetic Generic Binary Fixtures

This corpus contains deliberately non-text, non-Hermes binary pairs used to
lock down `chiff`'s generic binary path.

These fixtures exist to prove that even when no format-specific logic applies,
`chiff` still produces a correct patch and exposes an explicit `binary_pair`
engine reason.

Current pairs:

- `pairs/arbitrary-binary`
